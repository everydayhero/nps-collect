import { colors } from '../../traits'

export default {
  selected: {
    background: colors.blue,
    color: colors.white
  },
  radio: {
    position: 'absolute',
    clip: 'rect(0,0,0,0)'
  },
  label: {
    background: colors.white,
    color: colors.blue,
    display: 'inline-block',
    width: 40,
    lineHeight: '40px',
    textAlign: 'center',
    borderRadius: '50%'
  }
}
